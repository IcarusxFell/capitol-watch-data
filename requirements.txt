# Pinned for a stable, auditable supply chain (Dependabot keeps them current).
pypdf==6.14.2

# Optional OCR for the ~12% scanned House filings.
# Needs system packages too: tesseract-ocr + poppler-utils (installed in CI).
pdf2image==1.17.0
pytesseract==0.3.13
